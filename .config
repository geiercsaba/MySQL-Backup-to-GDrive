[GoogleDrive]
; Path to the file with the google drive credentials json
cred_file=./assets/credentials.json
; Google drive folder id where the backups will be stored (can be found in the url and need to be shared with the service account)
backup_folder=

[Database]
user=
password=
; list of databases to backup
databases=
